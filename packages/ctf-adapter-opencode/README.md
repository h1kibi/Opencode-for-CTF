# ctf-adapter-opencode

This package is reserved for OpenCode-specific wiring.

Planned responsibilities:
- map shared CTF core logic into OpenCode commands and tools
- keep harness-specific permission and agent definitions separate from reusable logic
- preserve compatibility with the current repository layout during migration
