---
description: "Compatibility alias for /ctf-expert (historical ctf-master entry). Prefer /ctf or /ctf-expert."
agent: ctf-expert
subtask: false
---

# /ctf-master — compatibility alias

`ctf-master` is **not** a third product mode. It redirects to **`ctf-expert`**.

Prefer:

- `/ctf` for auto-route
- `/ctf-expert` when you already know you need the evidence-driven lane

Challenge info:
$ARGUMENTS

Initialize CTF solving in `ctf-expert` mode:

- Recon → 3-route plan → sequential validation → evidence collection → iterate
- Maintain `Evidence.md` as the source of truth
- Team Mode only under `ctf-expert`
- For simple challenges, recommend `/ctf-fast` or `/ctf` instead
