---
description: Read and search a large source map without manual JSON scripting
agent: ctf-rev
subtask: false
---

Read a source map with `ctf-source-map-read`.

Use this when you need to:

- list `sources`
- extract `sourcesContent` by source name
- search a symbol across embedded source contents
