---
description: Read key fields from Godot text resources without dumping the whole file
agent: ctf-rev
subtask: false
---

Read key textual fields from `.tscn`, `.tres`, `.import`, `.remap`, or similar Godot text resources.

Target/context:
$ARGUMENTS

Use this to surface `node`, `script`, `text`, `signal`, and resource link lines quickly.
