---
description: "Compatibility alias for /ctf (historical ultrawork entry). Prefer /ctf."
agent: ctf-fast
subtask: false
---

# /ctf-solve — compatibility alias

Prefer **`/ctf`**. This command remains for older docs and muscle memory.

Challenge info:
$ARGUMENTS

Follow the same pipeline as `/ctf`:

1. Call `ctf-route-plan`.
2. Load returned skills / tool packs.
3. Solve on the chosen lane (fast or expert).
4. Stop only on flag, exhausted routes, or needed user resource.
