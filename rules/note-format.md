# Note Format Rules

Preferred workspace state files:

- `notes.md` for human-readable observations and command logs
- `.ctf-state.json` for resumable machine-readable challenge state

Keep `notes.md` compact and structured:

- challenge summary
- current phase
- hypothesis table
- primitive ledger when relevant
- command log with purpose, key output, interpretation, and fallback
