# Benchmark Integrity Rules

When the target is a benchmark or evaluator fixture:

- do not read hidden metadata as the solution
- do not use answer files unless the benchmark is explicitly about evaluator maintenance
- judge success from reproduced challenge behavior or explicit expected-behavior files
