# Retros

Store generated Web CTF retrospective reports here.

Expected naming: `<challenge-or-date>.md`.

Retro reports document what went well, what didn't, and what
lessons/patches were generated from a solve attempt. They feed
the `lessons/` and `patches/` directories.
