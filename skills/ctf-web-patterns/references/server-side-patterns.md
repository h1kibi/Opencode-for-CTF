# Server-Side Web CTF Patterns

SQLi filter/parser bypass, SSTI, SSRF, XXE, file read/write, command injection, deserialization, and upload/parser exploitation patterns.

Populated via retro pattern library updates and curated from external references.
