# Client-Side Web CTF Patterns

XSS, DOM, CSP, CORS, XS-Leak, admin bot, browser normalization, and JavaScript execution context patterns.

Populated via retro pattern library updates and curated from external references.
