# Authentication and Identity Web CTF Patterns

JWT, OAuth/OIDC, SAML, session, access control, token logic, and identity flow patterns.

Populated via retro pattern library updates and curated from external references.
